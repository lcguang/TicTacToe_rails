<h1>All Users</h1>

<div class="row">
  <div class="col-md-4">
    <div class="login-user">
      <% @users.each do |user| %>
        <% if user.online? %>
          <%= link_to user.name, '#' %>
        <% end %>
      <% end %>
    </div>
  </div>
</div>
